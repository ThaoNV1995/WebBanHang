@using System.Activities.Expressions
@using PagedList;
@using PagedList.Mvc
@model PagedList.IPagedList<MayTinhDaiPhat.Models.ChiTietHoaDonXuat>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Mã hóa đơn xuất
        </th>
        <th>
            Tên sản phẩm
        </th>
        <th>
            Số lượng
        </th>
        <th>
            Đơn giá 
        </th>
        <th>
            Chiết khấu
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td id="row_@item.MaCTHDX">
            @Html.DisplayFor(modelItem => item.HoaDonXuat.MaHDX)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SanPham.TenSP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoLuong)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DonGia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ChietKhau)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaCTHDX }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaCTHDX }) |
            @Ajax.ActionLink("Xóa", "Delete", new { id = @item.MaCTHDX }, new AjaxOptions()
                {
                    Confirm = "Bạn có muốn xóa bản ghi này không",
                    OnComplete = "$('#row_" + @item.MaCTHDX + "').remove()",
                    HttpMethod = "Delete"
                }, new { @class = "btn btn-danger btn-xs btn-edit" })
        </td>
    </tr>
}

</table>
